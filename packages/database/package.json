{
  "name": "@rs-inventory/database",
  "version": "1.0.0",
  "description": "Prisma ORM and SQLite database foundation for RS Inventory",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1",
    "@rs-inventory/types": "*"
  },
  "devDependencies": {
    "prisma": "^6.4.1",
    "typescript": "^5.8.2"
  }
}
